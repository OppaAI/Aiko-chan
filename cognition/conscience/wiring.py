"""Import-time wiring for Conscience Circuit Core.\n\nImport this module once at boot (system.wakeup) to patch:\n  - AikoThink.route          (CCC approval + respond gate)\n  - AikoThink._finalize_response (speak gate)\n  - agentic.execute_tool_with_policy (tool gate)\n\nPatches are idempotent and degrade to no-ops if targets are missing.\n"""\nfrom __future__ import annotations\n\nimport functools\nimport logging\n\nlog = logging.getLogger(__name__)\n_APPLIED = False\n\n\ndef apply() -> None:\n    global _APPLIED\n    if _APPLIED:\n        return\n    _APPLIED = True\n    try:\n        _patch_think()\n    except Exception as exc:\n        log.warning(\"[ccc-wiring] think patch failed: %s\", exc)\n    try:\n        _patch_agentic()\n    except Exception as exc:\n        log.warning(\"[ccc-wiring] agentic patch failed: %s\", exc)\n\n\ndef _patch_think() -> None:\n    from cognition.think import AikoThink\n    if getattr(AikoThink.route, \"_ccc_wrapped\", False):\n        return\n\n    orig_route = AikoThink.route\n    orig_finalize = AikoThink._finalize_response\n\n    @functools.wraps(orig_route)\n    def route(self, user_input: str, token_callback=None, system_note: str | None = None) -> str:\n        from system.userspace import current_user_id\n        user_id = current_user_id()\n        try:\n            from cognition.conscience.hooks import resolve_ccc_approval\n            ccc_reply = resolve_ccc_approval(user_input, user_id=user_id)\n            if ccc_reply is not None:\n                self._emit(ccc_reply, token_callback=token_callback)\n                return ccc_reply\n        except Exception as exc:\n            log.debug(\"[ccc-wiring] approval skipped: %s\", exc)\n\n        try:\n            from cognition.conscience.hooks import gate_respond\n            memorize = self._get_memorize()\n            mem_inner = getattr(memorize, \"_mem\", None) if memorize is not None else None\n            embedder = getattr(mem_inner, \"_embedder\", None)\n            decision, reply, note = gate_respond(\n                user_input=user_input,\n                user_id=user_id,\n                llm_client=getattr(self, \"_client\", None),\n                embedder=embedder,\n                surface=\"chat\",\n            )\n            if decision in (\"refuse\", \"escalate\") and reply:\n                self._emit(reply, token_callback=token_callback)\n                with self._history_lock:\n                    self._history.append({\"role\": \"user\", \"content\": user_input})\n                    self._history.append({\"role\": \"assistant\", \"content\": reply})\n                return reply\n            if decision == \"caution\" and note:\n                system_note = f\"{system_note}\\n{note}\".strip() if system_note else note\n        except Exception as exc:\n            log.debug(\"[ccc-wiring] respond gate skipped: %s\", exc)\n\n        return orig_route(self, user_input, token_callback=token_callback, system_note=system_note)\n\n    route._ccc_wrapped = True  # type: ignore[attr-defined]\n    AikoThink.route = route  # type: ignore[method-assign]\n\n    @functools.wraps(orig_finalize)\n    def finalize(self, user_input: str, draft: str, token_callback=None, *, already_emitted: bool = False) -> str:\n        try:\n            from cognition.conscience.hooks import gate_speak\n            memorize = self._get_memorize()\n            mem_inner = getattr(memorize, \"_mem\", None) if memorize is not None else None\n            embedder = getattr(mem_inner, \"_embedder\", None)\n            replaced = gate_speak(\n                draft=draft,\n                user_input=user_input,\n                llm_client=getattr(self, \"_client\", None),\n                embedder=embedder,\n                already_emitted=already_emitted,\n            )\n            if replaced is not None:\n                draft = replaced\n        except Exception as exc:\n            log.debug(\"[ccc-wiring] speak gate skipped: %s\", exc)\n        return orig_finalize(self, user_input, draft, token_callback, already_emitted=already_emitted)\n\n    finalize._ccc_wrapped = True  # type: ignore[attr-defined]\n    AikoThink._finalize_response = finalize  # type: ignore[method-assign]\n    log.info(\"[ccc-wiring] think.route + _finalize_response patched\")\n\n\ndef _patch_agentic() -> None:\n    import agentic.agentic as agentic_mod\n    if getattr(agentic_mod.execute_tool_with_policy, \"_ccc_wrapped\", False):\n        return\n    orig = agentic_mod.execute_tool_with_policy\n\n    @functools.wraps(orig)\n    def execute_tool_with_policy(name, args, state, owner=None, ctx=None, guards=None):\n        import json\n        try:\n            from cognition.conscience.hooks import gate_tool\n            from agentic.agentic import ToolResult, _append_step_trace, _SOCIAL_POST_TOOLS\n            owner_client = getattr(owner, \"_client\", None) if owner is not None else None\n            if owner_client is None and owner is not None:\n                owner_client = getattr(getattr(owner, \"owner\", None), \"_client\", None)\n            embedder = None\n            try:\n                memorize = None\n                if owner is not None and hasattr(owner, \"_get_memorize\"):\n                    memorize = owner._get_memorize()\n                if memorize is None and owner is not None:\n                    memorize = getattr(owner, \"_memorize\", None)\n                mem_inner = getattr(memorize, \"_mem\", None) if memorize is not None else None\n                embedder = getattr(mem_inner, \"_embedder\", None)\n            except Exception:\n                embedder = None\n            blocked = gate_tool(\n                name=name, args=args, llm_client=owner_client, embedder=embedder,\n                social_post_tools=_SOCIAL_POST_TOOLS,\n            )\n            if blocked is not None:\n                ctx = ctx or (owner if hasattr(owner, \"run_id\") else None)\n                trace = blocked.pop(\"as_trace\", None)\n                result = ToolResult(\n                    ok=False, tool=name, args=args,\n                    content=json.dumps(blocked, ensure_ascii=False),\n                    error_type=\"conscience_refuse\" if blocked.get(\"decision\") == \"refuse\" else \"needs_approval\",\n                    retryable=False,\n                    metadata={\"ccc\": trace or {}},\n                )\n                state.record(result)\n                if ctx is not None:\n                    _append_step_trace(ctx, \"conscience_gate\", blocked)\n                    _append_step_trace(ctx, \"tool_result\", {\"tool\": name, \"ok\": False, \"error_type\": result.error_type})\n                return result\n        except Exception as exc:\n            log.debug(\"[ccc-wiring] tool gate skipped: %s\", exc)\n        return orig(name, args, state, owner=owner, ctx=ctx, guards=guards)\n\n    execute_tool_with_policy._ccc_wrapped = True  # type: ignore[attr-defined]\n    agentic_mod.execute_tool_with_policy = execute_tool_with_policy\n    log.info(\"[ccc-wiring] agentic.execute_tool_with_policy patched\")\n\n\n# Apply on import so a single import in wakeup is enough.\napply()\n