# Deprecated — use `../shared/`

This `_shared` folder is kept temporarily for git history.

**Canonical location:** `interface/webui/studio/shared/`

All backends now mount `shared/` at `/shared`. New assets go under `shared/css/` and `shared/` (see `shared/README.md`).
